"""Sentry: recordings in, transcripts and summaries out.

The package is the import root rather than a distribution — ``src`` is on
the path and the entrypoints reach each other as :mod:`api`, :mod:`core`
and :mod:`connectors`.
"""
